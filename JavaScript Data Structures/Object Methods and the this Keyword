const car = {
  brand: "Toyota",
  model: "Camry",
  year: 2022,
  getCarInfo: function () {
    return `This is a ${this.year} ${this.brand} ${this.model}.`;
  }
};
// 2. Call the method
const carInfo = car.getCarInfo();
console.log(carInfo);
