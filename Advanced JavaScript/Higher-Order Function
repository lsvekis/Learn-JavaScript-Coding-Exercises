function applyOperation(x, y, operation) {
  return operation(x, y);
}
function add(a, b) {
  return a + b;
}
function subtract(a, b) {
  return a - b;
}
// Usage
console.log("Add:", applyOperation(5, 3, add));         // 8
console.log("Subtract:", applyOperation(5, 3, subtract)); // 2
